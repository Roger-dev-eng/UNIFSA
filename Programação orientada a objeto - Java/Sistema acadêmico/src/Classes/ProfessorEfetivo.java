package Classes;

public class ProfessorEfetivo extends Professor{
    public ProfessorEfetivo(String nome, int matricula, double salario) {
        setNome(nome);
        setMatricula(matricula);
        setSalario(salario);
    }
}
